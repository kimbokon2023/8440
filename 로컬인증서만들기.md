# 로컬 HTTPS 환경 구축 가이드

로컬 개발 환경에서 HTTPS를 사용하기 위한 완전한 가이드입니다.

## 필요성

- **Web Speech API** 같은 브라우저 API는 보안상 HTTPS 환경이 필요합니다
- 로컬에서도 실제 서버와 동일한 HTTPS 환경에서 테스트할 수 있습니다
- `localhost`는 예외적으로 HTTP에서도 작동하지만, 커스텀 도메인(예: `8440.local`)은 HTTPS가 필요합니다

---

## 1단계: mkcert 설치

### 방법 1: 수동 다운로드 (가장 간단, 관리자 권한 불필요)

1. GitHub 릴리즈 페이지 방문: https://github.com/FiloSottile/mkcert/releases
2. Windows용 파일 다운로드: `mkcert-v1.4.4-windows-amd64.exe`
3. 파일명을 `mkcert.exe`로 변경
4. 원하는 위치에 저장 (예: 프로젝트 폴더 또는 PATH가 설정된 폴더)

### 방법 2: Chocolatey (관리자 권한 필요)

```powershell
# PowerShell을 "관리자 권한으로 실행"
choco install mkcert
```

### 방법 3: Scoop (관리자 권한 불필요)

```powershell
# Scoop 설치 (처음 사용하는 경우)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
Invoke-RestMethod -Uri https://get.scoop.sh | Invoke-Expression

# mkcert 설치
scoop bucket add extras
scoop install mkcert
```

---

## 2단계: 로컬 CA 설치

시스템에 로컬 인증 기관(Certificate Authority)을 설치합니다. **이 작업은 최초 1회만 실행**하면 됩니다.

```bash
mkcert -install
```

**예상 출력:**
```
Created a new local CA 💥
The local CA is now installed in the system trust store! ⚡️
```

---

## 3단계: SSL 인증서 생성

사용하려는 도메인(들)에 대한 SSL 인증서를 생성합니다.

### 예시 1: 단일 도메인

```bash
cd c:/Project/mirae8440
mkcert 8440.local
```

### 예시 2: 여러 도메인 (권장)

```bash
cd c:/Project/mirae8440
mkcert 8440.local localhost 127.0.0.1 ::1
```

**생성되는 파일:**
- `8440.local+3.pem` - SSL 인증서 파일
- `8440.local+3-key.pem` - 개인 키 파일

**예상 출력:**
```
Created a new certificate valid for the following names 📜
 - "8440.local"
 - "localhost"
 - "127.0.0.1"
 - "::1"

The certificate is at "./8440.local+3.pem" and the key at "./8440.local+3-key.pem" ✅
It will expire on 5 February 2028 🗓
```

---

## 4단계: XAMPP Apache 설정

### 4-1. 인증서 파일을 XAMPP 디렉토리로 복사

```bash
# SSL 디렉토리 생성 (이미 있으면 생략)
mkdir -p c:/xampp/apache/conf/ssl.crt
mkdir -p c:/xampp/apache/conf/ssl.key

# 인증서 파일 복사
cp c:/Project/mirae8440/8440.local+3.pem c:/xampp/apache/conf/ssl.crt/
cp c:/Project/mirae8440/8440.local+3-key.pem c:/xampp/apache/conf/ssl.key/
```

### 4-2. httpd-ssl.conf 파일 수정

파일 위치: `c:/xampp/apache/conf/extra/httpd-ssl.conf`

파일 **맨 끝**에 다음 내용을 추가:

```apache
# 8440.local HTTPS VirtualHost
<VirtualHost *:443>
    ServerName 8440.local
    ServerAlias www.8440.local
    DocumentRoot "C:/Project/mirae8440/www"

    # SSL 설정
    SSLEngine on
    SSLCertificateFile "conf/ssl.crt/8440.local+3.pem"
    SSLCertificateKeyFile "conf/ssl.key/8440.local+3-key.pem"

    # 디렉토리 설정
    <Directory "C:/Project/mirae8440/www">
        AllowOverride All
        Require all granted
        Options Indexes FollowSymLinks
    </Directory>

    # PHP 파일에 SSL 환경 변수 활성화
    <FilesMatch "\.(cgi|shtml|phtml|php)$">
        SSLOptions +StdEnvVars
    </FilesMatch>

    # 로그 파일
    ErrorLog "logs/8440.local-ssl-error.log"
    CustomLog "logs/8440.local-ssl-access.log" common
</VirtualHost>
```

### 4-3. Apache 설정 검사

```bash
c:/xampp/apache/bin/httpd.exe -t
```

**예상 출력:**
```
Syntax OK
```

### 4-4. Apache 재시작

**방법 1: XAMPP Control Panel**
1. XAMPP Control Panel 열기
2. Apache의 "Stop" 버튼 클릭
3. "Start" 버튼 클릭하여 재시작

**방법 2: 명령줄 (관리자 권한 필요)**
```bash
net stop Apache2.4
net start Apache2.4
```

---

## 5단계: .gitignore 설정

인증서 파일이 Git에 커밋되지 않도록 `.gitignore` 파일에 추가합니다.

**`.gitignore` 파일에 추가:**

```gitignore
# SSL certificates (local development only)
*.pem
*.key
*.crt
*.csr
```

⚠️ **중요:** mkcert로 생성한 인증서는 **개발 환경 전용**입니다. 절대 실제 서버에 사용하지 마세요!

---

## 6단계: 테스트

브라우저에서 다음 URL로 접속합니다:

```
https://8440.local
```

### 성공 확인 사항:

✅ 브라우저 주소창에 🔒 자물쇠 아이콘이 표시됩니다
✅ 인증서 경고가 없습니다
✅ 사이트가 정상적으로 로드됩니다
✅ 개발자 도구(F12) → Console에 SSL 관련 오류가 없습니다

---

## 문제 해결

### 인증서 경고가 표시되는 경우

```bash
# mkcert CA가 제대로 설치되었는지 확인
mkcert -CAROOT

# CA 재설치
mkcert -uninstall
mkcert -install
```

### Apache가 시작되지 않는 경우

```bash
# 에러 로그 확인
tail -20 c:/xampp/apache/logs/error.log

# SSL 전용 에러 로그 확인
tail -20 c:/xampp/apache/logs/8440.local-ssl-error.log
```

**흔한 오류들:**

1. **포트 443이 이미 사용 중**
   - 다른 프로그램이 443 포트를 사용하고 있는지 확인
   - `netstat -ano | findstr :443` 으로 확인

2. **인증서 파일 경로 오류**
   - `httpd-ssl.conf` 파일의 경로가 정확한지 확인
   - 파일이 실제로 존재하는지 확인

3. **SSL 모듈이 활성화되지 않음**
   - `c:/xampp/apache/conf/httpd.conf` 파일에서 다음 라인이 주석 처리되지 않았는지 확인:
   ```apache
   LoadModule ssl_module modules/mod_ssl.so
   LoadModule socache_shmcb_module modules/mod_socache_shmcb.so
   Include conf/extra/httpd-ssl.conf
   ```

---

## 추가 도메인 설정

새로운 프로젝트에 HTTPS를 추가하려면:

### 1. 새 인증서 생성

```bash
cd c:/Project/새프로젝트명
mkcert 새도메인.local localhost 127.0.0.1 ::1
```

### 2. 인증서 복사

```bash
cp 새도메인.local+3.pem c:/xampp/apache/conf/ssl.crt/
cp 새도메인.local+3-key.pem c:/xampp/apache/conf/ssl.key/
```

### 3. httpd-ssl.conf에 VirtualHost 추가

위의 4-2 단계 내용을 참고하여 새로운 `<VirtualHost *:443>` 블록을 추가하되, 도메인과 경로를 새 프로젝트에 맞게 수정합니다.

### 4. Apache 재시작

---

## 참고 사항

- **인증서 유효기간:** 기본 3년 (예: 2025년 생성 시 2028년까지 유효)
- **로컬 전용:** 생성된 인증서는 해당 컴퓨터에서만 신뢰됩니다
- **팀 공유 불가:** 다른 팀원의 컴퓨터에서는 각자 mkcert 설정이 필요합니다
- **실제 서버 사용 금지:** 절대 프로덕션 환경에 사용하지 마세요

---

## 도움말 링크

- mkcert GitHub: https://github.com/FiloSottile/mkcert
- Apache SSL/TLS 문서: https://httpd.apache.org/docs/2.4/ssl/
- XAMPP 공식 문서: https://www.apachefriends.org/docs/

---

**작성일:** 2025-11-05
**버전:** 1.0
**테스트 환경:** Windows 11, XAMPP 8.x, Apache 2.4
