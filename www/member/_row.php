<?php
$id = isset($row["id"]) ? $row["id"] : '';  
$pass = isset($row["pass"]) ? $row["pass"] : '';  
$name = isset($row["name"]) ? $row["name"] : '';  
$level = isset($row["level"]) ? $row["level"] : '';  
$part = isset($row["part"]) ? $row["part"] : '';  
$hp = isset($row["hp"]) ? $row["hp"] : '';  
$numorder = isset($row["numorder"]) ? $row["numorder"] : '';  
$position = isset($row["position"]) ? $row["position"] : '';  
$eworks_level = isset($row["eworks_level"]) ? $row["eworks_level"] : '';
$division = isset($row["division"]) ? $row["division"] : '';
?>