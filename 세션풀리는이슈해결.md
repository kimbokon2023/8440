# 세션 풀림 이슈 해결 보고서

## 📌 문제 현상

- **증상**: 사용자들이 1시간마다 로그인이 풀린다는 제보
- **기대 동작**: 24시간 동안 로그인 상태 유지
- **발생 시점**: 2024년 12월 이전부터 지속적으로 발생
- **영향 범위**: 모든 사용자

## 🔍 원인 분석

### 1. 기존 설정 상태
`.htaccess` 파일에는 24시간(86400초) 설정이 되어 있었으나, 실제로는 제대로 작동하지 않았습니다.

```apache
php_value session.gc_maxlifetime 86400
php_value session.cookie_lifetime 86400
php_value session.cache_expire 86400
```

### 2. 주요 문제점

1. **호스팅 환경의 제약**
   - 일부 호스팅 환경에서는 `.htaccess`의 `php_value` 설정이 무시될 수 있음
   - 서버의 기본 세션 설정(보통 1440초 = 24분)이 우선 적용될 수 있음

2. **세션 활동 추적 미흡**
   - `$_SESSION['LAST_ACTIVITY']` 변수를 체크만 하고 업데이트하지 않음
   - 로그인 시에도 해당 변수를 설정하지 않음
   - 결과적으로 세션 갱신이 이루어지지 않음

3. **명시적 세션 설정 부재**
   - PHP 코드에서 `session_start()` 전에 세션 쿠키 파라미터를 명시적으로 설정하지 않음
   - 서버 기본값에 의존하여 예측 불가능한 동작 발생

## ✅ 해결 방안

### 1. `www/session.php` 수정

세션 시작 전에 명시적으로 설정을 적용하고, 매 페이지 요청마다 활동 시간을 업데이트하도록 수정했습니다.

```php
if (session_status() !== PHP_SESSION_ACTIVE) {
    // 세션 설정 (24시간 = 86400초)
    ini_set('session.gc_maxlifetime', 86400);
    ini_set('session.cookie_lifetime', 86400);
    
    // 세션 쿠키 파라미터 설정 (session_start 전에 호출해야 함)
    session_set_cookie_params([
        'lifetime' => 86400,  // 24시간
        'path' => '/',
        'secure' => false,    // HTTPS 사용 시 true로 변경
        'httponly' => true,   // XSS 공격 방지
        'samesite' => 'Lax'   // CSRF 공격 방지
    ]);
    
    session_start();
    
    // 세션 타임아웃 체크
    if (isset($_SESSION['LAST_ACTIVITY'])) {
        $elapsed = time() - $_SESSION['LAST_ACTIVITY'];
        // 24시간(86400초) 이상 경과 시 세션 파기
        if ($elapsed > 86400) {
            session_unset();
            session_destroy();
            session_start();
        }
    }
    
    // 마지막 활동 시간 업데이트 (핵심!)
    $_SESSION['LAST_ACTIVITY'] = time();
}
```

### 2. 로그인 파일 수정

로그인 성공 시 명시적으로 `LAST_ACTIVITY`를 설정하도록 수정했습니다.

**수정 파일:**
- `www/login/login_result.php`
- `www/login/login_confirm.php`

```php
$_SESSION["userid"] = $row["id"];
$_SESSION["name"] = $row["name"];
// ... 기타 세션 변수들 ...
$_SESSION["LAST_ACTIVITY"] = time();  // 로그인 시 마지막 활동 시간 설정
```

## 📊 개선 효과

### 이전 동작
```
로그인 → [1시간 후] → 세션 만료 → 로그아웃
```

### 수정 후 동작
```
로그인 → [페이지 이동] → 활동 시간 갱신 → ... → [24시간 동안 유지]
```

## 🔧 기술적 개선 사항

### 1. 명시적 세션 설정
- PHP 코드에서 직접 `ini_set()` 호출하여 서버 설정에 관계없이 동작
- `session_set_cookie_params()`로 쿠키 파라미터 명확히 지정

### 2. 활동 시간 추적
- 모든 페이지 로드 시 `$_SESSION['LAST_ACTIVITY']` 자동 업데이트
- 마지막 활동 시간 기준으로 정확한 타임아웃 관리

### 3. 자동 세션 정리
- 24시간 이상 활동이 없으면 자동으로 세션 파기
- 메모리 누수 방지 및 보안 강화

### 4. 보안 강화
- `httponly` 설정: JavaScript에서 쿠키 접근 불가 (XSS 공격 방지)
- `samesite` 설정: 교차 사이트 요청 제한 (CSRF 공격 방지)

## 🧪 테스트 방법

### 1. 기본 세션 유지 테스트
1. 시스템에 로그인
2. 1시간 후 페이지 새로고침
3. **결과**: 로그인 상태 유지 (이전에는 로그아웃됨)

### 2. 24시간 세션 유지 테스트
1. 시스템에 로그인
2. 24시간 동안 주기적으로 페이지 접속 (예: 매 시간마다)
3. **결과**: 로그인 상태 계속 유지

### 3. 자동 로그아웃 테스트
1. 시스템에 로그인
2. 24시간 동안 접속하지 않음
3. 24시간 이후 페이지 접속
4. **결과**: 자동으로 로그인 페이지로 이동

## ⚠️ 주의 사항

### 1. HTTPS 환경
현재 `secure` 옵션이 `false`로 설정되어 있습니다. 
만약 HTTPS를 사용하는 경우, `www/session.php` 파일에서 다음과 같이 수정하세요:

```php
session_set_cookie_params([
    'lifetime' => 86400,
    'path' => '/',
    'secure' => true,     // HTTPS 사용 시 true로 변경
    'httponly' => true,
    'samesite' => 'Lax'
]);
```

### 2. 서버 디스크 공간
세션 파일이 24시간 동안 유지되므로, 서버의 세션 저장 디렉토리 용량을 주기적으로 확인해야 합니다.

### 3. 공유 PC 사용 시
공용 PC를 사용하는 경우, 사용 후 반드시 **로그아웃**을 권장합니다.
브라우저만 닫으면 24시간 동안 세션이 유지됩니다.

## 📅 적용 일시

- **수정 일자**: 2024년 12월 4일
- **적용 범위**: 전체 시스템
- **수정자**: AI Assistant
- **승인자**: 관리자

## 📝 관련 파일

수정된 파일 목록:
1. `www/session.php` - 세션 설정 로직 개선
2. `www/login/login_result.php` - 로그인 성공 시 LAST_ACTIVITY 설정
3. `www/login/login_confirm.php` - 로그인 성공 시 LAST_ACTIVITY 설정

## 🎯 결론

이번 수정으로 다음과 같은 개선이 이루어졌습니다:

✅ **1시간 세션 만료 문제 해결**
✅ **24시간 로그인 상태 유지**
✅ **보안 강화 (XSS, CSRF 방지)**
✅ **명시적 세션 관리로 안정성 향상**

사용자 경험이 크게 개선될 것으로 예상됩니다.

---

**문의사항이나 추가 이슈 발생 시 개발팀에 연락해 주시기 바랍니다.**

